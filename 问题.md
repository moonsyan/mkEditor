# MarkdownSoft 项目代码审查报告

> 审查日期：2026-08-11
> 审查范围：`src/` 全部源代码（70+ 文件）

---

## 项目概览

**MarkdownSoft** 是一个基于 **Electron 43 + React 18 + Milkdown 7 (ProseMirror)** 的本地 Markdown 编辑器，支持多文件工作区、实时预览、多种导出格式（HTML/PDF/Word/EPUB/LaTeX）、图片托管等功能。

---

## 🔴 高严重度（3 项）

### 1. `app.whenReady()` 未处理 Promise 拒绝

**文件:** `src/main/index.ts:79`

```typescript
app.whenReady().then(initApp) // ❌ 没有 .catch()
```

**问题:** `initApp` 内部调用了 `registerIpcHandlers()`、`createWindow()` 等启动关键操作。如果任何一个抛出异常，Promise 被拒绝但无人处理，可能导致进程崩溃或产生难以排查的错误。

**建议:**
```typescript
app.whenReady().then(initApp).catch((err) => {
  console.error('App initialization failed:', err)
  app.quit(1)
})
```

---

### 2. `WebContents` 上的不安全 Monkey-Patching

**文件:** `src/main/window/window-manager.ts:100` / `src/main/ipc/handlers.ts:609`

```typescript
// handlers.ts — 写入
;(event.sender as unknown as { __unsaved?: boolean }).__unsaved = !!unsaved

// window-manager.ts — 读取（关闭前检查）
const unsaved = (wc as unknown as { __unsaved?: boolean }).__unsaved === true
```

**问题:** 使用三重 `as unknown as` 强制类型转换在 Electron 内部对象 `WebContents` 上挂载自定义属性。如果 Electron 内部结构变更，属性可能静默失效。多个 renderer 实例共享底层原型时状态可能错乱。

**建议:** 使用 `WeakMap<WebContents, boolean>` 替代 monkey-patching：
```typescript
const unsavedMap = new WeakMap<WebContents, boolean>()
// 写入
unsavedMap.set(event.sender, !!unsaved)
// 读取
const unsaved = unsavedMap.get(wc) === true
```

---

### 3. 表格列拖拽 `document` 级事件监听器泄漏

**文件:** `src/renderer/src/components/Editor/plugins/tableColResize.ts:175-181, 204-212`

```typescript
// mousedown 时注册到 document
document.addEventListener('mousemove', onDragMove)
document.addEventListener('mouseup', onDragEnd)

// destroy() 只清理了 editorView.dom 上的监听器
// 如果在拖拽过程中编辑器被卸载，document 级监听器永久泄漏！
```

**问题:** `destroy()` 方法只移除了 `editorView.dom` 上的事件，但 `onDragMove` 和 `onDragEnd` 是注册在 `document` 上的。如果在 mousedown 和 mouseup 之间组件被卸载，这两个监听器永远不会被移除。

**建议:** 保存 document 监听器的引用，在 `destroy()` 中也移除它们，或使用 `AbortController` 统一管理。

---

## 🟡 中严重度（6 项）

### 4. `as never` 绕过类型检查

**文件:** `src/renderer/src/components/Editor/index.tsx:251, 256`

```typescript
ctx.get(remarkPluginsCtx).push({
  plugin: footnoteRemarkPlugin as never, // ❌
  options: {},
})
ctx.get(remarkPluginsCtx).push({
  plugin: frontmatterRemarkPlugin as never, // ❌
  options: {},
})
```

**问题:** `footnoteRemarkPlugin` 和 `frontmatterRemarkPlugin` 的类型与 Milkdown 的 `remarkPluginsCtx` 期望类型不兼容，使用 `as never` 强制绕过。如果 Milkdown 升级导致内部 API 变化，会在运行时静默出错而没有编译时警告。

**建议:** 使用 `@ts-expect-error` 并附加注释说明原因，便于未来维护者复查：
```typescript
// @ts-expect-error — remark-footnote 的类型与 Milkdown 的 RemarkPlugin 不完全匹配，但运行时兼容
plugin: footnoteRemarkPlugin,
```

---

### 5. 模块级可变全局状态

**文件:**
- `src/renderer/src/components/Editor/plugins/searchHighlight.ts:13-20` — `searchState` 单例
- `src/renderer/src/components/Editor/plugins/codeLineNumbers.ts:20` — `lineNumbersEnabled` 单例
- `src/renderer/src/components/Editor/plugins/mermaidCodeBlock.ts:9-12` — `activePreviews` 等单例

```typescript
// searchHighlight.ts
export const searchState = {
  hits: [] as SearchHit[],
  current: -1,
  lastQuery: '',
  lastUseRegex: false,
  lastCaseSensitive: false,
  lastWholeWord: false,
}

// codeLineNumbers.ts
let lineNumbersEnabled = false
```

**问题:** 这些变量是模块级单例。在当前单编辑器架构下能工作，但如果 React StrictMode 双重挂载或未来支持多编辑器实例，会导致搜索高亮计数、代码行号状态等跨实例污染。

**建议:** 为 `searchState` 和 `lineNumbersEnabled` 使用 `WeakMap<EditorView, ...>` 将其作用域限定于编辑器实例。Mermaid 的模块级状态（`activePreviews`、`themeObserver`）因有正确的 `destroy()` 清理，可接受。

---

### 6. SearchBar `useEffect` 空依赖依赖外部 remount 机制

**文件:** `src/renderer/src/components/SearchBar/index.tsx:70-80`

```typescript
useEffect(() => {
  if (initial?.query) {
    onQueryChange(initial.query, initial.useRegex ?? false, ...)
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, []) // 故意空数组，依赖外部 key={searchEpoch} 机制重新挂载
```

**问题:** 这个设计依赖 `App.tsx` 中的 `key={searchEpoch}` 机制来重新挂载组件以获取新的初始化参数。如果 `key` 被意外移除，搜索初始化将静默失效。

**建议:** 用详细的设计注释替换 eslint-disable：
```typescript
// 初始化仅在挂载时执行一次。参数更新由父组件通过 key={searchEpoch} 重新挂载来实现。
// 请勿添加 deps——改动需要同步修改 App.tsx 中的 key 机制。
useEffect(() => { ... }, [])
```

---

### 7. `innerHTML` 直接注入（无防御层）

**文件:** `src/renderer/App.tsx:2119`

```typescript
content.innerHTML = editorRef.current?.getPreviewHtml() ?? ''
```

**问题:** 内容来自 ProseMirror 编辑器 DOM（当前可信），但没有 HTML 清理步骤。如果未来引入用户可控制的 HTML 渲染（如原始 HTML 块），可能成为 XSS 入口。

**同样问题** `src/renderer/src/components/Editor/plugins/mermaidCodeBlock.ts:151`:
```typescript
this.preview.innerHTML = svg
```

**建议:** 添加 DOMPurify 清理或信任边界注释，确保未来改动时不会引入风险。Mermaid 已配置 `securityLevel: 'strict'`，不要降级。

---

### 8. 启动初始化块缺少中断信号

**文件:** `src/renderer/App.tsx:295-583`

```typescript
useEffect(() => {
  // Promise.all(...).then(async () => {
  //   for (const entry of sessionFiles) { ... await ... }
  //   await restoreDrafts(...)
  // })
}, [])
```

**问题:** 启动时的 `useEffect([], ...)` 包含大量 `await` 调用（工作区文件恢复、草稿恢复等）。如果在启动过程中窗口关闭，所有异步操作继续运行并尝试对已卸载组件 `setState`。此外重试循环中的 `setTimeout(tryApply, 100)` 和 `setTimeout(openWhenReady, 100)` 也缺少清理。

**建议:** 添加 AbortController 或挂载标志检查，在组件卸载时中断异步操作。

---

### 9. 自动保存在卸载时无保护

**文件:** `src/renderer/App.tsx:750-797`

**问题:** `autoSaveInFlightRef` 防止了并发保存，但如果自动保存的异步操作正在进行时组件卸载，它仍然会尝试 `setSavedMap`、`setFileMtime`、`setToast`。

**建议:** 在自动保存逻辑开始时检查 mounted 标志。

---

## 🟢 低严重度（8 项）

### 10. 大量非空断言（`!`）依赖手动守卫

**文件:** `src/renderer/App.tsx:780, 1133, 1199, 1428` 等多处

```typescript
result.data!.modifiedTime // 依赖前置的 result.ok && result.data 守卫
```

**问题:** 技术上安全但脆弱——如果守卫逻辑被重构或复制粘贴出错，`!` 断言会变成运行时炸弹。

**建议:** 使用类型收窄辅助函数替代手动守卫：
```typescript
function assertOk<T>(r: { ok: boolean; data?: T }): r is { ok: true; data: T } {
  return r.ok && r.data !== undefined
}
```

---

### 11. 浏览器上下文中空转的自动保存定时器

**文件:** `src/renderer/App.tsx:754-798`

**问题:** 自动保存每 30 秒触发一次。handler 中有 `if (!window.desktopAPI) return` 守卫，但定时器本身在非 Electron 环境（浏览器调试）中也会持续创建和空转。

**建议:** 在创建定时器前检查 `window.desktopAPI` 是否存在。

---

### 12. GBK 编码往返校验可能产生误报

**文件:** `src/main/ipc/handlers.ts:409`

```typescript
if (iconv.decode(encoded, 'gbk') !== args.content) {
  return { ok: false, error: { code: 'ENCODING_LOSS', ... } }
}
```

**问题:** `iconv-lite` 的 encode→decode 往返可能因字符规范化（如全角/半角）产生误报，导致不必要的编码警告对话框弹出。后果仅为用户体验问题，不丢数据。

**建议:** 记录此行为或使用更精确的 Unicode 范围检测，而非依赖严格字符串相等。

---

### 13. SM.MS 上传缺少 fetch 超时

**文件:** `src/main/ipc/handlers.ts:1305`

```typescript
const resp = await fetch('https://sm.ms/api/v2/upload', { ... })
// ❌ 没有 AbortController 超时
```

**问题:** 如果 SM.MS API 挂起，IPC 调用将无限期阻塞。

**建议:**
```typescript
const controller = new AbortController()
const timer = setTimeout(() => controller.abort(), 30_000)
try {
  const resp = await fetch(url, { ..., signal: controller.signal })
} finally {
  clearTimeout(timer)
}
```

---

### 14. `sandbox: false` — 安全加固机会

**文件:** `src/main/window/window-manager.ts:39`

```typescript
webPreferences: {
  sandbox: false, // 因 preload 脚本使用了 process.platform
  contextIsolation: true,
  nodeIntegration: false,
```

**问题:** 当前禁用 sandbox 是因为 preload 脚本使用了 `process.platform`。将其迁移为 IPC 调用即可启用 sandbox，进一步加固渲染进程。

**建议:** 将 `process.platform` 通过 IPC 暴露给 renderer，然后设置 `sandbox: true`。

---

### 15. 文件保存 TOCTOU 竞态

**文件:** `src/main/ipc/handlers.ts:390-403`

**问题:** 保存文件时先 `stat` 检查冲突，然后通过 `writeFileAtomically`（临时文件 + rename）执行原子写入。这两个操作之间存在 TOCTOU 窗口——外部进程可能在 stat 之后、rename 之前修改文件。3 秒的 mtime 容差缓解了这个问题，但窗口仍然存在。

**建议:** 可接受（文件系统固有的限制），保持当前的 3 秒容差即可。

---

### 16. `Buffer.from` 解码错误信息不精确

**文件:** `src/main/ipc/handlers.ts:497, 1294`

```typescript
const buffer = Buffer.from(match[3], 'base64')
```

**问题:** 如果 base64 解码失败，外层 `try/catch` 会捕获但错误信息是泛化的 `IO_ERROR`，不利于调试。

**建议:** 在外层 catch 中检查是否为 base64 解码错误，提供更精确的错误码。

---

### 17. 打字机模式 `setTimeout(centerCaret, 0)` 可能冲突用户滚动

**文件:** `src/renderer/App.tsx:2380`

```typescript
if (next) setTimeout(centerCaret, 0)
```

**问题:** 这个延迟调用与 `cancelAnimationFrame` 是独立的调度路径，可能在用户手动滚动时触发冲突的滚动。

**建议:** 添加滚动来源标志，在用户手动滚动时跳过自动居中。

---

## ℹ️ 信息级（4 项）

### 18. MenuBar 索引用作 React key

**文件:** `src/renderer/src/components/MenuBar/index.tsx:203, 207`

菜单项是静态常量（`MENU_DEFS`），不会重排序，使用索引作为 key 可接受。

---

### 19. `contentEditable` 命令式 DOM 操作

**文件:** `src/renderer/App.tsx:2460-2473`

文档标题栏通过 blur 事件直接操作 DOM（`event.currentTarget.textContent = previousName`），绕过 React 状态管理。机制脆弱但当前可工作——如果 React 在重命名 IPC 完成前重新渲染，旧值会覆盖 DOM。

---

### 20. 搜索结果 key 唯一性依赖列表索引

**文件:** `src/renderer/src/components/WorkspaceSearchDialog/index.tsx:238`

```tsx
key={`${m.path}-${m.line}-${i}`}
```

同一文件同行的多个匹配因 index `i` 而保持唯一性，可工作但脆弱——更好的方案是使用匹配偏移量 `m.from`。

---

### 21. 自定义 CSS 注入（设计选择）

**文件:** `src/renderer/App.tsx:828-841`

用户通过对话框自选 CSS 文件并注入到文档中。作为桌面应用，用户导入自己的 CSS 是可接受的。但理论上恶意 CSS 可通过 `background: url(...)` 选择器泄露数据。

---

## ✅ 值得肯定的实践

| 实践 | 说明 |
|------|------|
| 原子写入 | `writeFileAtomically` 使用临时文件 + rename 模式防数据损坏 |
| 跨进程锁 | `acquireSettingsLock` 含 60 秒过期和 PID 校验，防止多窗口配置损坏 |
| 文件名校验 | `safeName` 函数拦截 `..`、非法字符（`\ / : * ? " < > \|`）、Windows 保留设备名（CON、PRN 等）和末尾点/空格 |
| 大小限制 | 20 余处大小/数量限制（文档 20MB、图片 20MB、CSS 1MB、搜索缓存 32MB、正则超时 500ms 等） |
| 路径遍历防护 | `realpath` + `relative` 检查防止 `mdimg://` 协议越权访问 |
| CSP 保护 | 严格的内容安全策略：`script-src 'self'`，无 `unsafe-eval` |
| 竞态防护 | `seqRef` 计数器和 `openingWorkspaceFilesRef` 去重机制防止并发冲突 |
| Worker 隔离 | 正则搜索在 Worker 线程中运行，带 500ms 超时保护 |
| Token 保护 | `imageHost` 密钥不可通过通用设置 API 读取，仅能通过专用通道配置 |
| 编码校验 | GBK 写入前进行 encode→decode 往返验证，失败时引导用户转 UTF-8 |
| 崩溃恢复 | 渲染进程崩溃自动重载（上限 3 次，带重置定时器） |
| 代码清洁度 | 零 `any` 类型、零 `@ts-ignore`/`@ts-expect-error`、零 `console.log`、零 `TODO`/`FIXME` |
| 错误处理模式 | 所有 IPC handler 统一返回 `{ ok, data?, error? }` 结构，错误码语义化 |
| 导航安全 | `will-navigate` 拦截 + `setWindowOpenHandler` 仅允许 http/https/mailto |
| 渲染器隔离 | `contextIsolation: true` + `nodeIntegration: false` + 严格的 preload API |

---

## 📊 量化统计

| 严重度 | 数量 | 涉及文件 |
|--------|------|----------|
| 🔴 高 | 3 | `src/main/index.ts`、`src/main/window/window-manager.ts`、`src/renderer/src/components/Editor/plugins/tableColResize.ts` |
| 🟡 中 | 6 | `Editor/index.tsx`、`searchHighlight.ts`、`codeLineNumbers.ts`、`SearchBar/index.tsx`、`App.tsx`、`mermaidCodeBlock.ts` |
| 🟢 低 | 8 | `handlers.ts`、`App.tsx`、`window-manager.ts` |
| ℹ️ 信息 | 4 | `MenuBar/index.tsx`、`App.tsx`、`WorkspaceSearchDialog/index.tsx` |

---

## 🔧 建议修复优先级

| 优先级 | 问题 | 文件 | 改动量 |
|--------|------|------|--------|
| **P0** | `app.whenReady()` 加 `.catch()` | `src/main/index.ts` | 1 行 |
| **P0** | Monkey-patching → WeakMap | `handlers.ts` + `window-manager.ts` | ~10 行 |
| **P0** | 拖拽监听器泄漏修复 | `tableColResize.ts` | ~5 行 |
| **P1** | `as never` → `@ts-expect-error` | `Editor/index.tsx` | 2 行 |
| **P1** | 全局状态 → 实例作用域 | `searchHighlight.ts` | ~15 行 |
| **P1** | 启动初始化加中断信号 | `App.tsx` | ~20 行 |
| **P2** | SM.MS fetch 加超时 | `handlers.ts` | ~6 行 |
| **P2** | 其他低严重度项 | 多个文件 | 各 ~5-15 行 |

---

## ✅ 复查与修复记录（2026-08-11 二次审查）

逐条对照实际代码复查后，修复 8 项、确认 13 项不成立或无需修改：

| # | 问题 | 结论 | 处理 |
|---|------|------|------|
| 1 | `app.whenReady()` 无 catch | ✅ 属实 | **已修复** — `index.ts:79` 增加 `.catch()` 记录错误并 `app.quit(1)` |
| 2 | WebContents monkey-patching | ✅ 属实 | **已修复** — 新建 `src/main/unsaved.ts`（`WeakMap<WebContents, boolean>`），`handlers.ts:610` 写入、`window-manager.ts:100` 读取全部改用该模块 |
| 3 | 表格列拖拽 document 监听器泄漏 | ❌ 不成立 | `tableColResize.ts:209-210` 的 `destroy()` 已移除 document 级监听器，无泄漏 |
| 4 | `as never` 绕过类型检查 | ✅ 属实 | **已修复** — 改为 `@ts-expect-error` + 原因注释（Milkdown 升级后类型兼容时会自动编译报错，提示清理） |
| 5 | 模块级可变全局状态 | ⚠️ 存在但无需修 | 单编辑器架构下安全且已注释说明；WeakMap 重构收益/风险比不佳 |
| 6 | SearchBar 空依赖 useEffect | ⚠️ 存在但已注释 | **已补注释** — 说明 `key={searchEpoch}` 重挂载机制，防止误删 deps |
| 7 | `innerHTML` 直接注入 | ⚠️ 信任边界已核实 | **已补注释** — 预览内容来自 ProseMirror 自身 DOM（无原始 HTML 透传）；Mermaid 为 strict 模式渲染。均标注未来引入原始 HTML 时须先清理 |
| 8 | 启动初始化无中断信号 | ❌ 不构成问题 | 重试循环有 2 秒上限；React 18 对卸载后 setState 为 no-op |
| 9 | 自动保存卸载无保护 | ❌ 不构成问题 | interval 在 effect cleanup 清除；卸载后 setState 为 no-op；`openFilesRef` 已有中途关闭守卫 |
| 10 | 大量非空断言 | ❌ 无需修 | 类型安全由前置守卫保证，属风格问题 |
| 11 | 浏览器上下文空转定时器 | ✅ 属实 | **已修复** — `App.tsx` 自动保存 effect 开头增加 `if (!window.desktopAPI) return`，非 Electron 环境不再创建定时器 |
| 12 | GBK 往返校验误报 | ❌ 不构成问题 | iconv 对可表示字符无损，往返校验精确；拒绝是保守设计 |
| 13 | SM.MS 上传无超时 | ✅ 属实 | **已修复** — `handlers.ts` 增加 `AbortController` 30 秒超时 |
| 14 | `sandbox: false` | ✅ 属实 | **已修复** — 启用 `sandbox: true`。preload 仅用 `process.platform`（沙箱化 preload 亦提供），经构建 + 真实 preload 冒烟测试验证 `desktopAPI` 正常暴露 |
| 15 | 保存 TOCTOU 竞态 | ❌ 无需修 | 报告本身判定可接受，保持 3 秒容差 |
| 16 | `Buffer.from` 解码错误不精确 | ❌ 不成立 | `Buffer.from(str, 'base64')` 对非法字符宽容、从不抛错，前提不成立 |
| 17 | 打字机模式滚动冲突 | ❌ 不构成问题 | 一次性调用；用户交互会取消浏览器 smooth scroll |
| 18-21 | 信息级 | ❌ 无需修 | 保持现状 |

**验证**：`tsc` 类型检查通过（仅测试文件的既有 vitest 模块解析报错）、`vitest` 31/31 通过、`electron-vite build` 成功、沙箱冒烟测试退出码 0。

---

## 🐛 用户反馈问题修复记录（2026-08-11 第三轮）

> 用户在真实使用（打开 Obsidian 库文件、连续撤销、切换文件夹）后反馈 3 个问题，均已修复。
> 验证：typecheck 干净（仅既有测试文件错误）、31/31 测试通过、electron-vite build 成功。

### 问题 A：Obsidian wiki 链接写法不兼容

**现象**：`D:\files\notes\obsidian\6. ai_know\MarkdownSoft V3\_index.md` 中的 `[[01 事故复盘/_index|01 事故复盘]]`（带路径 + 别名）不渲染为链接；保存含此类链接的文档直接失败。

**根因**（两个叠加，均已实证）：
1. `wikiTextConvertPlugin` 的 `view()` 只在编辑器创建时执行一次，切换文档（`replaceContent`）不触发 → 新文档的 `[[...]]` 不转换
2. `toMarkdown` runner 序列化 `wiki_link` 节点时 `addNode('wikiLink', ...)` → mdast-util-to-markdown 抛 `Cannot handle unknown node`（node 直跑复现）

**解决**：
1. 导出 `convertWikiTextInDoc(view)`，`replaceContent` 后手动调用（`src/renderer/src/components/Editor/plugins/wikiLink.ts`、`Editor/index.tsx:720-731`）
2. 序列化改 `state.addNode('html', undefined, '[[target|alias]]')` — 实证测试对比：text 节点被 `safe()` 转义（`\[\[...\_index...`），html 处理器零转义原样输出

**思路**：解析与序列化必须成对验证（load + save roundtrip），且用真实语料（Obsidian 实际文件）而非理想用例；Milkdown 序列化器是自研 API（无 `addText`），先读 `@milkdown/transformer` 源码 + 黑盒实验再下结论。

### 问题 B：多次 Ctrl+Z 内容乱、跨文档串内容

**现象**：连续撤销后内容混乱；切换文档后 Ctrl+Z 会把上一份文档的内容回退进来。

**根因**（两个叠加，均从源码确认）：
1. `replaceAll(markdown)` 默认 `flush=false` → 全文替换事务（`tr.replace(0, docSize, slice)`）进入 undo 历史 → 切文档后撤销回退到上一文档（`@milkdown/utils/lib/index.js:476`）
2. wiki 自动转换事务也进历史 → 历史栈被程序操作污染，多次撤销把链接还原成文本
3. Milkdown listener 的 `markdownUpdated` 使用 200ms 防抖；旧文档的延迟快照可能在切换后按当前文件 ID 回写，造成文件内容串联
4. 工作区文件异步读取完成顺序与点击顺序不一致，较早点击的慢请求可能在最后一次选择后反向激活

**解决**：
1. `replaceAll(markdown, true)` — flush 路径 `EditorState.create` + `view.updateState` 重建状态，**undo 历史清空**；已验证 listener 插件（自动保存/脏标记）在重建后仍正常
2. 转换事务 `tr.setMeta('addToHistory', false)` — 注意 prosemirror-history 新版已**不导出** `historyKey`，改用字符串 meta（`dist/index.js:274` 确认仍受支持）
3. 内容回调必须与当前编辑器序列化状态一致，否则丢弃防抖旧快照；工作区打开请求只允许最后一次选择意图激活编辑器

**思路**：程序自动执行的全文替换与用户编辑必须在 undo 语义上隔离——替换用状态重建（flush），自动转换用 meta 跳过历史；改内部 API 前先读实现源码，勿凭名称猜测。

### 问题 C：新文件夹默认全部展开

**现象**：打开新文件夹时所有子文件夹默认展开；期望默认折叠，仅"之前打开过且展开过"的文件夹维持原状。

**根因**：`sidebarCollapsedKeys` 初始 `[]`（空 Set = 全部展开），App 无法区分"没有记录"与"全部展开"。

**解决**：
1. App 初始值改 `null`（无记录），有记录才恢复（`App.tsx:842-846`）
2. `usePersistedSetting` 加 `value == null` 守卫，null 不写回
3. Sidebar 折叠规则（`Sidebar/index.tsx:211-243`）：
   - 无记录（null）→ 当前树全部文件夹加入折叠集合（默认全部折叠）
   - 记录与当前树完全不匹配（新工作区）→ 全部折叠；**不写回记录**（幂等），用户首次手动操作时记录自然更新为新工作区的键
   - 记录匹配 → 原样恢复
   - 边界：空数组 `[]`（用户曾展开全部）保持原样，不误判为"无匹配"

**思路**：持久化状态要用 `null` 表达"从未有值"的三态语义，避免 `[]` 歧义；"折叠集合"模型下恢复展开 = 不在集合中，跨工作区路径不匹配时按新工作区默认折叠处理。
