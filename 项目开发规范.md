# MarkdownSoft 项目开发规范

> 适用范围：本规范适用于 `mk-editormkEditor` 仓库的功能开发、缺陷修复、重构、测试、构建与发布。除非任务明确要求调整架构，本规范以当前已落地代码为准，不以 `docs/` 和 `develop/` 中的目标规划替代实际实现。

## 1. 项目基线

- 产品：MarkdownSoft，本地优先的桌面 Markdown 编辑器。
- 运行时：Electron 33；UI 使用 React 18；语言为 TypeScript 5，开启 `strict`。
- 构建：electron-vite + Vite；包管理与 CI 使用 npm，锁定文件为 `package-lock.json`。
- 编辑器：Milkdown 7（基于 ProseMirror），支持 CommonMark、GFM、代码高亮、数学公式与 Mermaid 图表。
- 样式：原生 CSS + CSS Variables；主题通过 `html[data-theme]` 切换。
- 支持平台：Windows、macOS、Linux；CI 使用 Node.js 20。

## 2. 架构与职责边界

应用按 Electron 的三进程模型组织，依赖和调用方向必须保持为：

```text
Renderer (React UI)
  -> window.desktopAPI
Preload (contextBridge 窄桥接)
  -> IPC
Main (系统能力、文件与窗口操作)
```

### 2.1 主进程：`src/main/`

- 只在主进程使用 Electron 主进程 API、Node.js 内置模块、文件系统、系统对话框、回收站、窗口与自动更新能力。
- `src/main/index.ts` 负责应用生命周期、协议注册、窗口级安全策略和主窗口创建；不在此堆积具体业务实现。
- 文件、工作区、导出、设置等 IPC 处理统一注册在 `src/main/ipc/handlers.ts`；新增系统能力应按现有分组添加处理器。
- 设置持久化只通过 `src/main/settings/settings-store.ts` 访问 `userData/settings.json`，不得让渲染进程自行读写应用设置文件。
- 窗口创建和窗口生命周期逻辑放在 `src/main/window/`，不得在渲染组件中绕过桥接直接控制窗口。

### 2.2 预加载层：`src/preload/`

- `src/preload/index.ts` 是唯一向页面暴露 Electron 能力的位置，统一挂载为 `window.desktopAPI`。
- 对外只暴露完成业务动作所需的最小方法，例如 `document.open`、`workspace.search`；禁止暴露通用 `ipcRenderer`、`invoke(channel)`、`fs`、`shell` 或 Node 全局对象。
- 新增或修改 API 时，必须同步更新 `src/preload/api.d.ts` 中的 `DesktopAPI`、入参、返回值和错误码类型。
- IPC 通道字符串只能定义在 `src/shared/ipc/channels.ts` 的 `CHANNELS` 中；主进程、预加载层、渲染层均引用该常量，禁止散落硬编码字符串。

### 2.3 渲染进程：`src/renderer/`

- `App.tsx` 是当前的应用编排层：持有会话、标签、文件内容、快捷键和顶层弹窗状态，负责把事件分发给组件与 `desktopAPI`。
- `src/renderer/src/components/` 放与视图绑定的组件；每个组件目录以 `index.tsx` 为入口，专属样式放在 `src/renderer/src/styles/components/` 的对应 CSS 文件中。
- `src/renderer/src/components/Editor/` 负责 Milkdown 的封装与编辑器插件。编辑器命令通过 `EditorHandle` 暴露，其他组件不得直接操作 ProseMirror 内部状态。
- `src/renderer/src/hooks/` 放可复用的 React 生命周期和状态逻辑；`src/renderer/src/lib/` 放不依赖 React 的纯工具和数据转换；`src/renderer/src/data/` 放静态演示数据、默认配置和快捷键定义。
- `src/renderer/src/env.d.ts` 负责 Vite 环境声明。渲染代码可使用 `@/` 指向 `src/renderer/src/`，配置来自 `electron.vite.config.ts` 与 `tsconfig.web.json`。

### 2.4 共享代码：`src/shared/`

- 只放主进程、预加载层和渲染层都需要的无副作用常量、DTO 与类型。
- 共享模块不得导入 `electron`、Node.js 内置模块、DOM API 或 React，避免跨进程构建耦合。

## 3. TypeScript 与代码风格

- 所有新业务代码使用 TypeScript，保持 `strict` 下无类型错误；不要以 `any`、`@ts-ignore` 或宽泛断言绕过类型问题。
- 组件、接口、类型、枚举使用 PascalCase；函数、变量、对象属性使用 camelCase；常量使用语义明确的 UPPER_SNAKE_CASE 或项目既有的 Pascal/camel 风格，同一模块保持一致。
- React 组件使用函数组件；事件处理函数以 `handle` 开头，例如 `handleSave`、`handleKeyDown`。可复用事件逻辑优先抽取为 hook 或命名函数。
- 导入按以下顺序组织：第三方包、项目绝对路径、相对路径、仅类型导入；同一来源的导入合并。类型仅使用时采用 `import type`。
- 条件分支优先使用早返回和 `continue`，避免深层嵌套。`}` 后换行，`catch` 是唯一可与右花括号同一行的例外。
- 注释说明原因、边界或不可直观看出的约束，不复述代码行为。用户可见文案、注释、文档默认使用简体中文。
- 当前项目未配置 ESLint、Prettier；修改代码时必须延续现有无分号、两空格缩进、单引号、尾随逗号的书写风格。引入新的格式化或检查工具须同时补齐 npm 脚本与 CI。

## 4. 状态、数据与文件规则

### 4.1 状态归属

- 编辑器文本和选区由 Milkdown/ProseMirror 管理；`App.tsx` 的 `contents`、`savedMap`、`fileMtime` 是其会话快照，不能形成第二套彼此独立的编辑器状态。
- 组件内部短生命周期状态（展开、悬停、输入框值）留在组件中；跨顶层区域共享的会话状态由 `App.tsx` 编排。状态膨胀到难以维护时，再以功能边界拆分 hook，不应随意增加全局状态库。
- 供异步回调、键盘监听或编辑器回调读取的最新状态，沿用当前 `useRef` 镜像模式，避免闭包读取过期状态。
- 所有持久化设置通过 `window.desktopAPI.settings.get/set` 访问；首次加载完成前不得覆盖用户已有配置。

### 4.2 文件与内容安全

- Markdown 文件是用户数据。保存、另存为、重命名、移动、删除、导入图片和导出均必须保留明确的成功、取消和失败分支，并向用户反馈。
- 覆盖磁盘文件时必须传递并更新 `expectedMtime`，遵循主进程已有的外部修改冲突检测；收到 `CONFLICT` 时不得静默覆盖。
- GBK 源文件保存时应保留编码信息；收到 `ENCODING_LOSS` 时不得丢弃不可映射字符，应让用户选择安全处理方式。
- 破坏性操作优先使用系统回收站。新增删除能力不得直接永久删除用户工作区内容，除非用户明确确认且功能说明已写明。
- 草稿、会话、最近文件和写作统计属于应用状态，与正文文件分离存储；不得将 token、绝对路径、草稿正文或用户隐私写入仓库、日志和错误上报。

## 5. IPC 与 Electron 安全

- 渲染进程不得导入或调用 `fs`、`path`、`child_process`、`electron` 等 Node/Electron 权限 API。
- 每个 IPC handler 必须校验入参类型、允许范围和关联窗口；对用户提供的文件名、路径、正则、Data URL、HTML 和 CSS 设置容量、格式或行为边界。
- IPC 返回结构沿用 `{ ok: true, data }` 或 `{ ok: false, error: { code, message? } }`。错误码必须稳定、可由渲染层处理，不能只返回未分类的异常文本。
- 通道与 `DesktopAPI` 需要同步演进：先定义 `CHANNELS`，再实现主进程 handler、预加载方法和类型声明，最后从渲染层调用。
- `contextIsolation` 必须保持启用，`nodeIntegration` 必须保持关闭。禁止为了便利向渲染层泄露任意 IPC、文件路径访问或 Node 能力。
- 外部链接仅允许安全协议并经 `shell.openExternal` 打开；新增自定义协议必须在 `app.whenReady()` 前注册特权，并定义可访问资源边界。
- 任何来自 Markdown、导入 CSS、图床、HTML 导出或网络响应的内容，在进入 DOM、打印窗口或文件系统前都要按用途处理转义、白名单或体积限制。禁止用 `dangerouslySetInnerHTML` 绕过来源校验。
- 图床 token 只存放在主进程设置中，严禁写入前端常量、文档示例、Git 历史或日志。

## 6. React 组件与交互规范

- 组件职责单一。页面编排留在 `App.tsx`，复杂面板按现有 `SettingsDialog` 方式拆分子面板；不因一次性逻辑创建没有边界的公共组件。
- Props 应明确类型；回调名表达动作与结果，例如 `onOpenFile`、`onChange`、`onClose`。不要通过多个布尔值表达互斥状态，优先使用联合类型。
- 所有异步 UI 操作都要处理加载、防重复提交、取消、失败与组件卸载后的状态更新。对文件选择、导出、图片上传等操作，取消不应显示为错误。
- 快捷键定义集中在 `src/renderer/src/data/shortcuts.ts`，动作由 `App.tsx` 统一分发；新增动作必须同步更新默认快捷键、设置面板、帮助内容和冲突处理逻辑。
- 可交互元素使用语义化 `button`、`input`、`dialog` 等标签；图标按钮提供 `aria-label` 或可访问名称；菜单、弹窗和拖拽操作要支持键盘关闭、焦点恢复和必要的键盘操作。
- 渲染大量文件、搜索结果或图片时，设置数量、文件大小和递归深度上限，避免主线程阻塞。已有主进程搜索和目录扫描的规模守卫不得被删除或绕过。

## 7. 编辑器与 Markdown 规则

- 编辑器扩展优先在 `src/renderer/src/components/Editor/plugins/` 中实现，并在 `Editor/index.tsx` 集中注册；不要在页面组件中临时修改 Milkdown schema 或 ProseMirror transaction。
- 新增语法或插件必须验证 Markdown 的解析、编辑、序列化回写和重新打开后的兼容性，避免用户正文发生不可逆变更。
- 编辑器命令应通过既有命令式接口调用；命令执行后以编辑器回调同步到会话状态，不手工修改与编辑器不一致的文本副本。
- 图片路径转换统一使用 `src/renderer/src/lib/image-path.ts`；PDF/HTML 导出使用现有转换与转义逻辑，不能复制一套不一致的路径或 HTML 处理规则。
- 对公式、Mermaid、代码块、脚注、frontmatter、表格和任务列表的变更，要覆盖至少一个正常示例、一个包含中文的示例和一个异常/不完整输入示例。

## 8. 样式与主题规范

- 全局基础规则放在 `styles/global.css`；共享设计变量放在 `styles/variables.css`；排版规则放在 `styles/typography.css`；组件样式放在 `styles/components/`；主题色和值放在 `styles/themes/`。
- 新增颜色、边框、阴影、圆角、间距和动效，优先复用或新增语义化 CSS Variable，例如 `--bg-surface`、`--text-2`、`--accent`，不要在组件 CSS 中散落同一视觉语义的硬编码值。
- 主题应覆盖全部新增变量，并在 default、dark、ocean、rose 下检查文本、边框、禁用态、悬停态、焦点态和系统标题栏可读性。
- 样式选择器应以组件根类名为范围，避免不受控的标签选择器污染 Milkdown 内容和其他对话框。不要用 `!important` 解决层级问题，除非是覆盖第三方编辑器样式且注释说明原因。
- UI 应以高频写作为中心，保持紧凑、可扫描、不遮挡内容。桌面窗口尺寸变化时保证工具栏、弹窗、侧栏、标签栏与状态栏不重叠，文本不能溢出控件。

## 9. 文档与目录管理

- 根目录 `README.md` 维护产品简介、安装、常用命令和关键架构入口；`docs/` 维护面向使用者和维护者的说明；`develop/` 与 `项目/` 可保存设计、学习和规划资料。
- 修改用户可感知功能、快捷键、导出格式、设置项、目录职责或安全边界时，必须同步更新相应文档。
- 文档必须以 UTF-8 编码保存。仓库已有部分历史文档在非 UTF-8 PowerShell 默认读取下会显示乱码，新增或改写文档一律显式使用 UTF-8，并避免扩大无关编码改动。
- `out/`、`release/`、`node_modules/` 是构建或依赖产物，不编辑、不提交。应用图标等可发布资源统一放在 `resources/`。

## 10. 质量验证

### 10.1 当前最低门槛

- 每次代码变更至少执行 `npm run build`，确保 main、preload 和 renderer 的 TypeScript/Vite 构建均通过。
- 修改 UI、编辑器、IPC、文件操作或打包配置时，还必须执行 `npm run dev` 进行人工冒烟验证；关闭开发窗口后再结束进程，避免残留 Electron 实例。
- 当前仓库尚未配置项目测试框架，也没有 `src/` 下的测试文件；不得声称已运行不存在的 `npm test`。涉及高风险逻辑时，应在变更中补充可运行的测试方案或记录手工验证步骤。

### 10.2 必测场景

- 文件：打开、保存、另存为、外部修改冲突、取消操作、中文路径、GBK/UTF-8 编码、删除到回收站。
- 工作区：打开目录、嵌套 Markdown 文件、创建、重命名、移动、全文搜索、超大文件/大量文件的限制行为。
- 编辑器：输入与撤销重做、切换标签、保存状态、Markdown 往返、图片、表格、代码块、公式和 Mermaid。
- 会话：自动保存、草稿恢复、最近文件、设置恢复、关闭含未保存内容的窗口、多窗口模式。
- 视觉：四套主题、深浅对比、不同窗口宽高、键盘导航、焦点样式、弹窗和菜单层级。
- 打包：触及主进程、依赖、资源、构建配置或平台行为时，至少验证目标平台的安装包启动、打开文件、保存与导出。

## 11. 构建、依赖与发布

```bash
npm ci             # 按 package-lock.json 安装 CI 一致依赖
npm run dev        # 启动 Electron 开发环境
npm run build      # 构建 main、preload、renderer
npm run preview    # 预览构建产物
npm run build:win  # 构建 Windows NSIS 安装包
npm run build:mac  # 构建 macOS DMG
npm run build:linux # 构建 Linux AppImage
```

- 本地开发推荐 Node.js 20，最低要求遵循 `README.md` 的 Node.js 18+；CI 固定 Node.js 20，依赖升级必须在该版本下验证。
- 依赖调整使用 npm，并同步提交 `package.json` 与 `package-lock.json`。禁止手工编辑 lockfile；升级 Electron、electron-builder、Milkdown 等核心依赖前须验证构建和关键编辑流程。
- `electron-builder` 的产物目录、应用 ID、产品名、图标和平台目标以 `package.json` 的 `build` 字段为准。修改发布配置时同步检查 `.github/workflows/build.yml` 的产物路径。
- GitHub Actions 在 `v*` 标签推送后构建 Windows/macOS/Linux，并创建草稿 Release。发布前需确认版本号、标签、三个产物和自动更新地址均正确；未配置真实更新服务时不得承诺自动更新可用。

## 12. Git 协作规则

- 提交前查看 `git status` 和差异，只提交与当前需求直接相关的源文件、文档、配置及必要锁文件；不覆盖或回退他人的未提交改动。
- 分支默认以 `codex/` 前缀创建，分支名使用小写英文和连字符，表达需求目的。
- 提交信息必须采用：`<type>: <摘要> #<PM号>`。`type` 仅允许 `feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`。
- 提交前至少完成本规范第 10 节要求的验证，并在提交说明、PR 或工作记录中说明执行的命令与未覆盖的风险。
- 提交和 PR 应保持小而完整：一个变更聚焦一个可验证目的；架构重构、依赖升级与功能调整需要分开，除非它们无法独立构建。

## 13. 变更检查清单

- [ ] 变更位于正确的进程、目录和职责边界内。
- [ ] 新增 IPC 已同步通道常量、主进程 handler、preload 方法和 `api.d.ts`。
- [ ] 文件操作、异步操作和取消/失败分支已处理，未静默丢失用户内容。
- [ ] 新增 UI 具备语义、键盘与焦点支持，并已检查四套主题。
- [ ] 新增编辑器能力已验证 Markdown 往返与重新打开后的结果。
- [ ] 文档、快捷键、设置说明和构建配置已按影响范围同步。
- [ ] `npm run build` 已通过；涉及交互的功能已完成开发环境冒烟验证。
- [ ] 未提交 `node_modules/`、`out/`、`release/`、密钥、用户路径或草稿数据。
