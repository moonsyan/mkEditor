# MarkdownSoft 功能文档与 Typora 对比分析

> 版本：v0.0.8  
> 更新日期：2026-08-09（v0.0.8：修复文件树移动前写回后基线/mtime 未同步导致的假脏状态与误报冲突；至此文档所列问题与功能全部闭环）

---

## 一、当前已实现功能

### 1.1 编辑器核心

- **所见即所得 Markdown 编辑**：基于 [Milkdown](https://milkdown.dev)（ProseMirror 内核），输入 Markdown 语法即时渲染
- **标准 Markdown 语法**（commonmark preset）：标题（H1–H6）、粗体、斜体、删除线、行内代码、链接、图片
- **GitHub Flavored Markdown**（gfm preset）：表格、任务列表、代码块
- **列表类型**：无序列表、有序列表、任务列表（`- [ ]` / `- [x]`）
- **代码块语法高亮**：基于 Prism.js，支持语言标识（如 ` ```python `）
- **脚注**：`[^label]` 引用 + `[^label]: 内容` 定义，支持输入规则实时转换
- **数学公式（KaTeX）**：行内 `$...$` 与块级 `$$...$$`，按需懒加载插件
- **流程图/图表（Mermaid）**：` ```mermaid ` 代码块，按需懒加载插件
- **撤销/重做**：基于 history 插件

### 1.2 文件管理

- **新建文档**：在内存中创建未命名文档（`未命名 N.md`）
- **打开文件**：对话框选择 `.md` / `.markdown` 文件
- **保存 / 另存为**：支持自定义格式过滤器（导出 HTML 等）
- **打开文件夹（工作区模式）**：扫描目录生成 Markdown 文件树
- **文件树操作**：新建文件（自动编号）、重命名、删除（移至回收站）
- **右键上下文菜单**：新建 / 重命名 / 复制路径 / 删除
- **内联重命名**：在树中直接编辑文件名
- **拖入 .md 文件打开**：拖拽文件到编辑区直接打开
- **自动保存**：每 30 秒自动写回已打开的磁盘文件
- **外部冲突检测**：基于 mtime 的冲突检测，弹出"是否覆盖"确认
- **草稿恢复**：崩溃/异常退出后恢复未保存内容（草稿持久化到设置）
- **会话恢复**：重启后自动打开上次关闭的文件和工作区
- **最近文件**：记录最近 10 个打开的磁盘文件，出现在"文件"菜单

### 1.3 界面与主题

- **自定义菜单栏**：非原生菜单，集成在窗口顶栏，无系统菜单栏干扰
- **4 套主题**：暖白（default）、墨夜（dark）、海雾（ocean）、玫砂（rose）
- **自定义主题**：设置 → 外观 → 导入自定义 CSS（注入 `<style>` 覆盖内置样式，可移除，持久化）
- **标题栏覆盖层颜色同步**（Windows）：窗口控制按钮区跟随主题色
- **可编辑文档标题**：顶部栏直接修改，回车确认
- **侧栏宽度可调**：拖拽调整，宽度 180–480px
- **4 档字号**：小（sm）/ 标准（md）/ 大（lg）
- **3 档内容宽度**：窄 / 标准 / 宽
- **3 档行距**：紧凑 / 标准 / 宽松
- **3 种内容字体**：默认 / 衬线 / 等宽
- **代码块行号**：设置开关，装饰 widget 实现（不修改文档，导出时一并包含）
- **代码块全屏预览**：悬浮层展开按钮，只读全屏查看 + 复制，Esc 关闭
- **文件编码自动探测**：打开时先按 UTF-8 严格解码，失败自动转 GBK（Electron 内置 full-icu）；状态栏展示源编码；GBK 文件保存/自动保存/移动前写回均按原编码写回（iconv-lite）；内容含 GBK 无法表示的字符（如 emoji）时拒绝静默写入，手动保存确认后/后台自动降级为 UTF-8（内容永不丢失）；另存为统一 UTF-8
- **整体缩放**：70%–180%，支持 Ctrl+滚轮、Ctrl+=、Ctrl+-
- **专注模式**（F11）：隐藏侧栏、菜单、状态栏，仅保留编辑区
- **分栏预览模式**（Ctrl+Shift+P）：左侧编辑，右侧实时预览，滚动联动
- **打字机模式**：光标所在段落始终保持在屏幕中央

### 1.4 侧栏与导航

- **文件树 Tab**：工作区文件树（支持展开/折叠、已打开标记）
- **大纲 Tab**：从 Markdown 标题构建的目录树，支持嵌套折叠
- **大纲跟随高亮**：光标所在标题在大纲中高亮
- **点击大纲定位**：点击标题平滑滚动至对应位置（带高亮闪烁效果）
- **文件打开标记**：已打开文件旁显示小圆点

### 1.5 查找替换

- **查找栏**：支持正则表达式、大小写敏感、全字匹配（`ab` 开关）
- **全工作区搜索**：文件菜单"全工作区搜索…"，跨全部 .md 文件逐行匹配（子串 + 正则 + 大小写可选），点击结果打开文件并自动带入查询词继续定位（规模守卫：500 文件/单文件 2MB/200 条结果）
- **匹配计数**：显示 `x / y`（当前位置 / 总数）
- **逐个跳转**：上一个 / 下一个匹配项，自动滚动到可视区
- **替换当前**：替换当前匹配项并跳转至下一个
- **全部替换**：一次性替换所有匹配项
- **搜索高亮**：ProseMirror 装饰方式高亮，非 DOM 篡改
- **自动排除代码块**：搜索/替换不匹配 `code_block` 内容

### 1.6 导出

- **导出 HTML**：生成自包含 HTML，本地图片自动内联为 base64
- **导出 PDF**：隐藏窗口渲染后使用 `printToPDF`，支持纸张尺寸（A4/Letter/A5/Legal）、三档页边距、页眉页脚（标题+页码）、首页目录（H1–H3 可跳转锚点）
- **导出 Markdown**：另存为新的 `.md` 文件（加 `-导出` 后缀避免覆盖）
- **pandoc 多格式导出**（基础框架）：文件菜单"导出 Word / EPUB…"，自动检测本机 pandoc；未安装时提示安装指引
- **图片管理**：扫描 `attachments/` 目录，显示本地图片列表
- **图床上传**（基础框架）：设置中可选 SM.MS 图床（填 Token），粘贴/拖入图片直接上传；未配置或失败时自动降级本地附件

### 1.7 状态栏

- 保存状态（● 未保存 / 已保存）
- 当前所在位置（光标上方最近的标题）
- 光标行/列号
- 选中字符数（有选区时）
- 字数、行数、预计阅读时间
- 文档最后修改时间
- 编码（UTF-8）、格式（Markdown）

### 1.8 设置

- **外观**：主题切换、字号、内容宽度、行距、字体、缩放、自定义主题 CSS 导入
- **编辑器**：自动保存开关、打字机模式开关、拼写检查（多语言词典可选：英/法/德/西/意/葡/荷/俄）、多窗口模式、空白点击行为、代码块行号、图床配置（本地/SM.MS）
- **快捷键**：所有核心快捷键可自定义，支持冲突检测与恢复默认；新增插入链接（Ctrl+K）/图片（Ctrl+Shift+I）/代码块（Ctrl+Alt+C）/引用（Ctrl+Alt+Q）/分割线（Ctrl+Alt+R）

### 1.8b 表格编辑

- **表格悬浮工具栏**：悬停表格时右上角显示操作按钮：加行 / 加列 / 删行 / 删列 / 删表 / 左中右对齐（光标不在表内时自动移入）
- 菜单"段落"中保留插入表格 / 加行 / 加列 / 删除选中单元格命令

### 1.9 帮助

- **快捷键一览**：展示当前所有快捷键绑定
- **Markdown 语法**：常见语法快速参考
- **写作统计**：今日/累计字数、写作时长（含 30 天历史曲线）
- **关于**：版本信息

### 1.10 自动更新

- 生产环境自动检查更新（electron-updater），静默下载，提示重启安装

---

## 二、Mermaid 支持情况

**已支持，但存在已知限制。**

### 支持方式
- 依赖 `@milkdown/plugin-diagram`（v7.7.0）+ `mermaid`（v11.16.1）
- **按需懒加载**：编辑器检测到 Markdown 内容中含 `` ```mermaid `` 时自动加载插件，不污染启动内存
- 编辑器创建完成后若初始内容含 Mermaid，也会触发加载

### 已知限制

| 问题 | 说明 | 状态 |
|------|------|------|
| 懒加载时序 | 首次切换含 Mermaid 的文档时，需等待插件加载（约 200–500ms），期间图表占位为纯文本 | 部分缓解：加载完成后立即重渲染并刷新预览 |
| 导出 HTML 时 Mermaid 渲染 | ~~若插件未加载完成，快照中无 SVG~~ | ✅ 已修复：导出前强制等待插件就绪（最多 4 秒） |
| 导出 PDF 时 Mermaid 渲染 | 同上 | ✅ 已修复：同上，导出前统一等待富内容就绪 |
| 分栏预览 Mermaid | ~~依赖 DOM 快照，时序问题~~ | ✅ 已修复：插件渲染完成后自动刷新预览快照 |

---

## 三、已知 Bug 与体验问题

> v0.0.2 修复进度：B1/B2/B3/B5/B6/B7 已修复（B4 待引入多语言拼写库）；U1–U8 全部已修复。

### 3.1 严重问题

**B1. Mermaid / KaTeX 懒加载时序问题** ✅ 已修复（首次加载跳变仍存在，属懒加载固有特性）
- 编辑器新增 `ensureRichContent()` 接口：导出 HTML/PDF 前强制等待插件装载并重渲染（最多 4 秒），导出期间有轻提示
- 插件渲染完成后通过 `onRichRender` 回调立即刷新分栏预览快照

**B2. 分栏预览的内容刷新有 200ms 延迟** ✅ 已修复
- 改用 `requestAnimationFrame`，内容变化后下一帧即取 DOM 快照

### 3.2 中等问题

**B3. 搜索不区分代码块内外** ✅ 已修复
- `collectHits` 遍历文档时跳过 `code_block` 节点，代码块内容不再被匹配高亮/替换

**B4. 拼写检查仅支持英文** ⚠️ 部分改善
- 已支持选择 Electron 内置词典语言（英/法/德/西/意/葡/荷/俄），主进程校验语言可用性后设置
- 中文拼写检查仍不支持（Chromium 无中文词典；如需需引入 cspell 等第三方库）

**B5. 自动保存冲突检测的 mtime 容差仅 1 秒** ✅ 已修复
- 容差已扩大至 3 秒，覆盖 FAT32（2 秒精度）等低精度文件系统

**B6. 草稿恢复的基线可能不准确** ✅ 已修复
- 新增 `file:stat` IPC：恢复每个磁盘文件草稿前重新 stat，若 mtime 与读取时偏差超 3 秒则丢弃草稿，以磁盘最新内容为准

**B7. 多窗口模式下会话可能互相覆盖** ✅ 已修复
- settings 写入改为主进程串行队列（读-改-写原子执行），并发写不再交错损坏；fresh 窗口仍不写会话

### 3.3 体验问题

**U1. 打字机模式滚动精度不高** ✅ 已修复
- 优先用光标所在行的矩形（`Range.getClientRects()`）居中，无法取到时回退块级居中；偏移小于 2px 不滚动，消除连续输入抖动

**U2. 代码块悬浮层在窗口缩放时定位偏移** ✅ 已修复
- 悬浮层改用内容坐标（offsetTop/offsetLeft 叠加）定位，与 `getBoundingClientRect` 不同，不受整体缩放/侧栏宽度变化影响

**U3. 状态栏修改时间格式缺少秒** ✅ 已修复
- 状态栏时间格式改为 `YYYY-MM-DD HH:mm:ss`

**U4. 搜索栏无"高亮全部匹配"的持久化** ✅ 已修复
- 查询词/正则/大小写/替换文本持久化到 settings（`searchState`），重新打开搜索栏自动恢复并重新执行搜索

**U5. 文件树无拖拽排序/移动功能** ✅ 已修复
- 工作区文件树支持拖拽文件/文件夹到目标文件夹（新增 `file:move` IPC）；禁止移入自身/子目录，移动前自动写回未保存内容，已打开文件的 id/基线/mtime 同步迁移

**U6. 新建文件后未自动聚焦编辑区** ✅ 已修复
- 新建/打开文件后额外延迟一帧再聚焦（等内容替换与渲染完成），确保可直接输入

**U7. 工作区文件右键菜单缺少"在新窗口中打开"** ✅ 已修复
- 右键菜单新增"在新窗口打开"；新窗口以 fresh 模式启动并通过 `#fresh?file=` 携带路径，启动后自动打开该文件

**U8. 编辑器区域空白处点击行为** ✅ 已修复（可配置）
- 设置 → 编辑器新增"点击空白区跳到文末"开关（默认开启，与 Typora 一致），不习惯的用户可关闭

---

## 四、与 Typora 功能对比

### 4.1 对标维度总览

| 维度 | MarkdownSoft | Typora | 差距 |
|------|:---:|:---:|:---:|
| **核心编辑** | ✅ 所见即所得 | ✅ 所见即所得 | 基础体验接近 |
| **Markdown 语法覆盖** | ✅ 完整（含 GFM） | ✅ 完整（含 GFM + 扩展） | 接近 |
| **数学公式** | ✅ KaTeX（懒加载） | ✅ KaTeX（内置） | 功能相当 |
| **流程图/图表** | ⚠️ Mermaid（懒加载，导出/预览时序已修复） | ✅ Mermaid / PlantUML（稳定） | 首次加载跳变仍存在 |
| **文件管理** | ✅ 基本完整 | ✅ 更丰富的文件组织 | 差距较小 |
| **主题与外观** | ✅ 4 套主题 + 自定义 CSS 导入 | ✅ 10+ 主题 + 自定义 CSS | 接近 |
| **导出** | ✅ HTML / PDF / Markdown / pandoc（需安装） | ✅ 更多格式（Word、LaTeX 等） | 依赖本机 pandoc |
| **搜索替换** | ✅ 正则 + 大小写 + 全字 + 排除代码块 + 工作区全文搜索 | ✅ 更智能的搜索体验 | 基本相当 |
| **专注/打字机模式** | ✅ | ✅ | 相当 |
| **分栏预览** | ✅ | ✅ | 相当 |
| **大纲导航** | ✅ | ✅ | 相当 |
| **快捷键自定义** | ✅ | ❌（固定） | MarkdownSoft 更灵活 |
| **自动保存** | ✅ | ❌（手动保存） | 各有侧重 |
| **会话恢复** | ✅ | ✅ | 相当 |
| **图片管理** | ⚠️ 本地 + SM.MS 图床（基础框架） | ✅ 支持图床、拖拽上传 | 差距缩小 |
| **云端同步** | ❌ | ✅ Typora Cloud | 缺失 |
| **协作编辑** | ❌ | ❌ | 均无 |
| **插件系统** | ❌ | ❌ | 均无 |
| **拼写检查** | ⚠️ 9 种内置词典语言可选（无中文） | ✅ 多语言 | 差距缩小 |
| **自动更新** | ✅ | ✅ | 相当 |

### 4.2 未实现的重要功能（对标 Typora）

#### 高优先级

1. **更稳定的 Mermaid 渲染** ✅ 已完成（v0.0.2）
   - 导出前强制等待插件就绪，预览在插件渲染完成后自动刷新；仅首次加载跳变仍属懒加载固有特性

2. **图片管理增强** ⚠️ 基础框架已完成
   - 已支持 SM.MS 图床（设置中填 Token，粘贴/拖入自动上传，失败降级本地）
   - 待扩展：更多图床提供商（阿里云 OSS、Imgur 等）

3. **多格式导出** ⚠️ 基础框架已完成
   - 已支持通过 pandoc 导出 Word / EPUB / LaTeX / 纯文本（自动检测安装，未安装时提示指引）
   - 待扩展：内置 pandoc 分发或更多格式

4. **代码块体验优化** ✅ 已完成
   - 定位偏移已修复（v0.0.2）；行号开关（装饰 widget）；全屏预览（只读 + 复制，Esc 关闭）

5. **搜索增强** ✅ 已完成
   - 搜索跳过代码块（v0.0.2）；全字匹配开关（`ab` 按钮，与正则/大小写组合生效）

#### 中优先级

6. **更多导出格式与选项** ✅ 已完成
   - PDF 导出选项弹窗：纸张尺寸（A4/Letter/A5/Legal）、三档页边距、页眉页脚（标题+页码）、首页目录（H1–H3 可跳转）

7. **文件树拖拽** ✅ 已完成（v0.0.2）

8. **主题自定义** ✅ 已完成
   - 设置 → 外观 → 导入/移除自定义 CSS，注入 `<style>` 覆盖内置主题，持久化到 settings

9. **写作统计增强** ✅ 已实现（此前版本已有，本轮校正记录）
   - 帮助 → 写作统计：今日/累计字数、时长 + 近 7 天柱状图 + 近 5 周日历热力图

10. **表格编辑增强** ✅ 已完成
    - 悬停表格工具栏：加行/加列/删行/删列/删表 + 当前列左/中/右对齐（setAlignCommand）
    - 未做：Typora 风格的单元格边框拖选（依赖编辑器内核改造，投入产出比低）

#### 低优先级

11. **云端同步** ❌ 未实现
    - 需账号体系与存储服务，不在本地编辑器范畴，留待后续评估

12. **拼写检查多语言** ⚠️ 部分完成
    - 已支持 9 种内置词典语言选择；中文仍无词典（同 B4）

13. **更多快捷键** ✅ 已完成
    - 新增：插入链接 Ctrl+K、插入图片 Ctrl+Shift+I、代码块 Ctrl+Alt+C、引用 Ctrl+Alt+Q、分割线 Ctrl+Alt+R；均可在设置中自定义

14. **文件编码选择** ✅ 已完成（自动探测 + 原编码写回 + 丢字保护）
    - 打开时 UTF-8 严格解码失败自动回退 GBK，状态栏展示源编码；保存/自动保存/重命名/移动前写回均按原编码写回（GBK 经 iconv-lite）；写入前往返校验，含 GBK 无法表示字符时手动保存确认后/后台自动降级 UTF-8；另存为统一 UTF-8

15. **全文搜索（工作区级）** ✅ 已完成（基础版）
    - 文件菜单"全工作区搜索…"：跨 .md 逐行匹配（子串/正则，非法表达式有提示），结果预览高亮命中片段，点击结果打开文件并把查询词带入文档内搜索；并发搜索仅采纳最后一次结果
    - 待扩展：后台索引

---

## 五、项目架构总结

```
MarkdownSoft (Electron + React + Milkdown)
├── 主进程 (src/main)
│   ├── index.ts          — 入口，协议注册，单实例锁，自动更新
│   ├── ipc/handlers.ts   — 全部 IPC 处理器（文件/窗口/设置/图片）
│   ├── settings/         — JSON 配置文件读写
│   └── window/           — 窗口管理（多窗口支持）
├── 预加载 (src/preload)  — contextBridge 安全暴露 API
├── 渲染进程 (src/renderer)
│   ├── App.tsx           — 主组件（状态管理 + 业务逻辑）
│   ├── components/
│   │   ├── Editor/       — Milkdown 编辑器（含搜索/图片/Mermaid/KaTeX）
│   │   ├── MenuBar/      — 自定义菜单栏
│   │   ├── Sidebar/      — 文件树 + 大纲面板
│   │   ├── StatusBar/    — 底部状态栏
│   │   ├── SearchBar/    — 查找替换栏
│   │   ├── SettingsDialog/ — 设置弹窗
│   │   ├── HelpDialog/   — 帮助弹窗
│   │   ├── ImagesDialog/ — 图片管理弹窗
│   │   └── ThemeSwitcher/ — 主题切换按钮
│   └── styles/           — CSS 变量主题系统（4 套主题）
└── 包依赖
    ├── @milkdown/kit     — 编辑器内核
    ├── @milkdown/plugin-math  — KaTeX 公式（懒加载）
    ├── @milkdown/plugin-diagram — Mermaid 图表（懒加载）
    ├── @milkdown/plugin-prism   — 代码高亮
    ├── katex             — 公式渲染
    ├── mermaid           — 图表渲染
    └── electron-updater  — 自动更新
```

---

## 六、下一步建议

1. ~~**修复 Mermaid 懒加载时序**~~ — ✅ 已完成（导出前等待插件就绪，预览加载后自动刷新）
2. **图片图床支持** — 接入 SM.MS 等免费图床，提升写作流畅度
3. **接入 pandoc 多格式导出** — 大幅扩展导出能力
4. ~~**代码块搜索排除**~~ — ✅ 已完成（搜索跳过 code_block 节点）
5. **写作统计可视化** — 加入图表展示写作习惯
