# 需求文档：公开需求信息采集工具

> 版本：v0.1（需求初稿）
> 日期：2026-08-11
> 状态：待确认（文末「待确认问题」需要决策）

---

## 1. 项目背景与目标

**一句话目标**：从公开渠道采集「用户需求」类信息（求助帖、评论区中的需求表达），经大模型提炼成「呼声最高的需求清单」，以此作为产品选题依据——由本人亲自开发对应的程序/网站/小程序，并把**自己开发的软件**分享给大家。

**核心链路**：公开信息采集（个人市场调研）→ 大模型提炼需求清单 → 本人开发需求对应的程序 → 分享自研软件

**典型使用场景**：
- 搜索关键词（副业/兼职、求职、程序员接单、工具软件、生活求助），收集相关公开求助帖
- 收集帖子下的公开评论，评论往往是真实需求的集中表达
- 大模型把帖子+评论提炼成「呼声排序的需求清单」
- 本人基于清单开发对应程序，并分享自己开发的软件

**明确的非目标**（防止范围失控）：
- 不对外发布、展示、转售平台**原始数据**——分享的永远是**自己开发的软件**，平台帖子/评论原文只作内部调研输入
- 不在自研产品中原样搬运平台帖子/评论原文（只用提炼后的摘要）
- 不公开帖子作者的个人信息
- 不做数据可视化大屏、不做推荐算法
- 不采集任何登录后可见的私有内容

---

## 2. 合规原则（最高优先级，红线）

用户明确要求「不想有任何法律风险」。以下原则为硬性约束，所有功能设计必须满足：

### 2.1 硬性红线（绝不做）
1. **不绕过任何反爬技术措施**：不逆向签名算法、不伪造设备指纹、不识别验证码、不破解加密参数
2. **不采集私有数据**：只采集无需登录即可公开访问的内容
3. **不规模化**：单机、低频、百条/天量级，不对平台造成任何可感知负载
4. **不涉及个人信息滥用**：不采集手机号、微信号等联系方式；用户名默认脱敏存储
5. **不对外传播数据**：数据仅本地个人使用

### 2.2 主动合规措施（要做）
1. 采集前检查目标平台的 **robots.txt** 和用户协议，评估是否允许
2. **官方 API 优先**：平台提供官方开放接口的，优先走官方渠道（如 B站开放平台）
3. 每平台独立限速，全局并发上限，遵守合理的访问间隔
4. 数据保留期限制，定期清理
5. 版本发布前自查平台条款是否更新

### 2.3 风险认知（诚实说明）
- 任何非官方渠道的采集（即使只是读公开内容），在平台用户协议层面都可能被禁止——这是**合同风险**，无法完全归零
- 但只要遵守 2.1 的红线，**法律风险（被起诉/刑责）可视为可忽略**，平台封号风险也极低
- 真正做到「条款层面也干净」的唯一方式：**全程使用平台官方 API**
- 本项目交付物定位：**采集数据仅供内部市场调研，最终对外分享的是本人开发的软件，不含平台原始数据**——「公开数据调研 → 自研产品」是正常的产品开发行为
- 结论：本项目以「个人研究用途 + 低频 + 公开内容 + 不绕过任何防护」为边界，实质法律风险极低；如某平台条款明确禁止（见 3.3 微博），需用户知情决策

---

## 3. 数据来源与渠道分级

### 3.1 B站 —— 绿色通道（推荐首选）
| 项 | 说明 |
|---|---|
| 渠道 | 官方开放平台 API + 公开接口（搜索、视频信息、评论） |
| 可采集 | 关键词搜索的视频/专栏列表、弹幕、热评 |
| 风险等级 | 低（官方提供接口，鼓励开发者接入） |
| 备注 | 第一版首选，用于验证整套框架 |

### 3.2 贴吧 —— 绿色通道
| 项 | 说明 |
|---|---|
| 渠道 | 公开网页接口（无需登录可读） |
| 可采集 | 帖子标题、正文、楼层回复 |
| 风险等级 | 低（公开论坛信息，需求类帖子密度高） |
| 备注 | 需检查百度 robots.txt；低频访问 |

### 3.3 微博 —— 黄色通道（需用户决策）
| 项 | 说明 |
|---|---|
| 渠道 | 公开页面/接口（搜索需登录态） |
| 可采集 | 公开博文、评论 |
| 风险等级 | 中（微博用户协议明确禁止未经授权抓取） |
| 备注 | 两个选项：① 申请微博开放平台 API（合规但门槛高，个人申请受限）；② 放弃微博，用其他渠道替代。**不建议**用登录态绕过其协议 |

### 3.4 暂缓平台（二期再评估）
- **小红书 / 抖音**：强风控 + 登录墙，且无公开搜索接口，与「零风险」原则冲突，本期不做
- **求职网站 / 接私活平台**：数据价值密度低、登录墙严重，本期不做；如需岗位需求数据，可考虑聚合站或官方招聘数据源

---

## 4. 功能需求

### FR-1 关键词任务管理
- 维护关键词列表（如「副业」「接单」「求助」「有没有人帮我」「工具推荐」）
- 每个关键词可配置：适用平台、启停状态、采集深度（只看帖子 / 帖子+评论）
- 支持命令行方式添加/删除关键词

### FR-2 采集流程（每个平台）
1. 按关键词搜索 → 得到帖子列表
2. 抓取帖子正文（标题、正文、作者、时间、链接）
3. 按需抓取公开评论
4. 全部写入本地数据库

### FR-3 数据字段（统一结构）
| 字段 | 说明 |
|---|---|
| platform | 来源平台 |
| keyword | 触发该帖的关键词 |
| post_id | 帖子唯一ID（用于去重） |
| title / content | 标题 / 正文 |
| author | 作者名（脱敏：仅存昵称，不存主页链接） |
| post_time | 发布时间 |
| url | 帖子链接（只存链接，不存内容截图） |
| comments | 评论列表（内容 + 时间） |

### FR-4 去重
- 以 post_id 为唯一键，重复采集自动跳过

### FR-5 存储
- 本地 SQLite 单文件存储，无需部署数据库

### FR-6 浏览与导出
- 命令行方式按平台/关键词查看采集结果
- 支持导出 CSV / Markdown 清单

### FR-7 限速与安全
- 每平台独立限速（默认 ≥10 秒/请求），全局并发 ≤3
- 失败自动重试（最多 2 次），长时间失败自动暂停该任务并告警

### FR-8 日志
- 记录每次采集的任务、数量、失败原因，可回溯

### FR-9 插件化扩展
- 平台采集器做成独立模块，后续新增平台只写一个插件，不动主框架

### FR-10 大模型需求提炼（核心功能）
- 对采集的帖子+评论，调用大模型（如 Claude API）提炼需求清单
- 输出结构：需求描述 / 呼声强度（同类表达出现次数）/ 代表场景 / 来源平台与链接
- 呼声排序：相同诉求的不同表达自动归并、统计频次，让「大家都需要什么」一目了然
- 提炼结果存库，可与原始数据对照溯源
- 需要 API Key，由使用者自行配置（个人承担调用费用）

### FR-11 需求看板（本地网页界面）
- 本地启动的网页看板（无需部署服务器）：
  - 按平台/关键词浏览原始帖子与评论
  - 查看 AI 提炼的需求清单（按呼声强度排序）
  - 一键导出 CSV / Markdown 报告

### FR-12 定期需求报告
- 按周/月生成「需求趋势报告」：新增需求、呼声上升的需求、来源分布

---

## 5. 非功能需求

- 单机运行，个人电脑可跑（Python 3.10+）
- 数据量级：每天数百条以内
- 对目标平台负载：可忽略
- 无外部依赖服务（除可选 AI 提炼外）
- 程序运行结束自动退出，不做常驻服务

---

## 6. 架构概要

```
D:\project\spider\
├── 需求文档.md          # 本文档
├── core/               # 框架：任务调度、限速、去重、重试、SQLite 存储
├── sources/            # 平台插件（每个平台一个模块）
│   ├── bilibili.py
│   └── tieba.py
├── analyze/            # 大模型提炼模块（需求清单生成）
├── web/                # 本地需求看板
├── keywords.json       # 关键词任务配置
├── data/               # SQLite 数据库文件、导出文件
└── cli.py              # 命令行入口
```

**数据流**：关键词任务 → 调度器 → 平台插件（搜索→帖子→评论）→ 解析为统一字段 → 去重 → SQLite → 查询/导出

---

## 7. 迭代计划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| 阶段 0（当前） | 需求确认 | 本文档待确认问题全部拍板 |
| 阶段 1 | B站插件 + 框架 + SQLite | 用 3 个关键词跑通「搜索→帖子→评论→去重→导出」全流程 |
| 阶段 2 | 贴吧插件 | 贴吧数据入库，字段统一 |
| 阶段 3 | AI 需求提炼 | 用真实数据跑出第一份需求清单，验证提炼质量 |
| 阶段 4 | 本地需求看板 + 周报 | 网页看板上线，周报可生成 |
| 阶段 5 | 微博方案（按决策执行） | 官方 API 或放弃 |

---

## 8. 需求确认结论（2026-08-11，全部拍板）

1. ✅ 核心链路：采集 → 大模型提炼呼声清单 → **本人开发需求对应的程序并分享自研软件**
2. ✅ 数据用途：仅作内部市场调研输入；对外分享的只有本人开发的软件，不含平台原始数据
3. ✅ 界面形式：本地网页看板（查看帖子/评论、需求清单、导出报告）
4. ✅ AI 提炼：核心功能（自备 API Key，费用自担）
5. ✅ 关键词主题：副业/兼职、求职、程序员接单、工具软件、生活求助（后续可增删）
6. ✅ 微博：暂时放弃（B站 + 贴吧起步）
7. ✅ 数据保留：默认 14 天（2 周）后清理

**需求冻结，进入开发规划。**

---

## 9. 风险登记

| 风险 | 等级 | 缓解措施 |
|---|---|---|
| 平台接口变动导致插件失效 | 中 | 插件独立封装，坏一个不影响整体；定期维护 |
| 平台条款收紧 | 中 | 每次大版本更新前自查条款 |
| 微博 ToS 明令禁止抓取 | 低 | 已放弃微博（2026-08-11 确认），此风险消除 |
| 误采集个人信息 | 低 | 字段白名单制，联系方式字段根本不采集；对外分享的软件不含平台用户信息 |
| 平台内容著作权 | 低 | 自研产品只用提炼摘要，不搬运帖子/评论原文 |
| 对外分发平台数据 | 已消除 | 交付物为自研软件，不含平台原始数据（2026-08-11 确认） |
