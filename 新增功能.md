# 新增功能：Obsidian 风格 Wiki 链接、YAML 属性面板、文件树状态持久化

> 实施日期：2026-08-11
> 基于项目：MarkdownSoft (Electron 43 + React 18 + Milkdown 7)

---

## 一、背景与需求

用户希望为 MarkdownSoft Markdown 编辑器加入三个 Obsidian 风格功能：

1. **`[[文件链接]]` 语法** — 输入 `[[` 自动触发文件搜索弹窗，渲染为可点击链接，点击跳转到目标文件
2. **YAML 属性面板** — 解析文档 `---` 包裹的 frontmatter 元数据，以表格展示并可编辑
3. **文件树状态记忆** — 默认折叠，记住展开/折叠和侧边栏活动标签页，重启后恢复

---

## 二、设计思路

### 2.1 整体架构

项目采用 Electron 主进程/渲染进程分离架构，所有文件操作通过 IPC 通信。编辑器内核为 Milkdown 7（基于 ProseMirror），通过插件系统扩展。

**关键设计原则：**
- 遵循现有代码风格：TypeScript 严格模式、无分号、2 空格缩进、单引号
- 复用现有模式：Milkdown 插件系统、`usePersistedSetting` 持久化、EditorHandle ref API
- 零新增 npm 依赖——YAML 解析使用自写简单解析器、Wiki 链接不加 micromark 扩展（用文本扫描转换方案）
- 保持安全模型：CSP 保护、渲染器隔离

### 2.2 Wiki 链接 `[[link]]` 设计

**输入规则方案：**
- 用户输入 `[[target]]` → ProseMirror InputRule 匹配 `]]` 闭合 → 替换为 `wiki_link` atom 节点
- 用户输入 `[[target|alias]]` → 同样匹配，alias 作为显示文本

**文档加载转换方案（核心创新）：**
- 由于 micromark 扩展类型复杂度高，采用 **文档加载后批量转换** 策略：
  - `wikiTextConvertPlugin` 在编辑器视图创建后扫描所有文本节点
  - 用正则 `/\[\[...\]\]/g` 匹配，替换为 `wiki_link` 节点
  - 递归调用 `setTimeout(convert, 0)` 逐个转换（避免 ProseMirror 事务位置漂移）
  - 跳过 `code_block` 和 `frontmatter` 内的文本

**切换文档后重新转换（2026-08-11 修正）：**
- `wikiTextConvertPlugin` 的 `view()` 只在编辑器创建时执行一次，切换标签页（`replaceContent`）不会再次触发 → 新文档中的 `[[...]]` 仍是纯文本（Obsidian 兼容 bug #1）
- 修复：导出 `convertWikiTextInDoc(view)`，`replaceContent` 替换内容后手动调用，立即转换新文档
- 附带修复：转换循环用模块级 `currentView` 跟踪活跃视图，编辑器销毁后链式 `setTimeout` 自动停止（避免对已销毁视图 dispatch）

**序列化方案（2026-08-11 修正）：**
- 初版 `toMarkdown` runner 用 `state.addNode('wikiLink', ...)` → 生成未知 mdast 节点，mdast-util-to-markdown 抛 `Cannot handle unknown node` → 保存含 wiki 链接的文档直接失败（Obsidian 兼容 bug #2）
- 修复：改用 `state.addNode('html', undefined, '[[target|alias]]')` —— html 处理器原样输出、零转义
- 为什么不用 text 节点：实证测试确认 mdast-util-to-markdown 的 `safe()` 会把 phrasing 上下文中的 `[`、`_` 转义成 `\[\[...\_index...`，保存后链接失效
- 为什么不用自定义 mdast 扩展：Milkdown 的 SerializerState 是自研 AST 栈，注册自定义节点的链路复杂；html 节点是验证过的最短路径

**撤销历史处理（2026-08-11 修正）：**
- 转换事务（自动、非用户操作）设 `tr.setMeta('addToHistory', false)` 不进 undo 历史，否则多次 Ctrl+Z 会把链接还原为文本
- 注意：新版 prosemirror-history 不再导出 `historyKey` PluginKey，但字符串 meta `'addToHistory'` 仍被 `filterTransaction` 支持（已在 node_modules 源码确认）

**点击跳转方案：**
- `wikiLinkClickPlugin` 使用 ProseMirror `props.handleClick` 事件委托
- 检测点击元素的 `.wiki-link` class，读取 `data-target` 属性
- 通过模块级 ref 回调通知 React 层

**自动补全方案：**
- `wikiAutocompletePlugin` 检测 `[[` 输入 → 提取 query 文本
- 通过模块级回调通知 React → 渲染 `WikiAutocomplete` 浮层（参照 `EditorOverlays` 的 codePanel 模式）
- 用户选择 → 替换文本范围为 `wiki_link` 节点

### 2.3 YAML 属性面板设计

**解析策略：**
- 项目已有 `frontmatter.ts` 将 `---` 块解析为 ProseMirror 节点
- 新增 `frontmatter-parser.ts` 提供纯函数：`parseFrontmatterYaml(text)` / `formatFrontmatterYaml(props)` / `replaceFrontmatter(md, props)`
- 简单键值对解析（覆盖 90%+ 使用场景），复杂 YAML 回退为原始文本

**编辑策略：**
- 属性编辑 → `replaceFrontmatter()` 生成新 Markdown → `editorRef.replaceContent()` 替换全文
- 替代方案（精准 ProseMirror 节点替换）保留为未来优化方向

**UI 策略：**
- 置于编辑器上方（仿 Obsidian Properties 位置），可折叠
- `FrontmatterProperties` 组件：键/值/删除按钮三列布局，底部新增行

### 2.4 文件树状态持久化设计

**控制反转方案：**
- Sidebar 现有 `collapsedKeys: Set<string>` 和 `activeTab` 内部状态
- 改为受控模式：接收 `initialCollapsedKeys` / `activeTab` props 和 `onCollapsedKeysChange` / `onActiveTabChange` 回调
- App.tsx 管理状态 → `usePersistedSetting` 写入 settings.json

**默认折叠方案（2026-08-11 修正）：**
- 初版：`initialCollapsedKeys` 缺省为空数组 → 空 Set 表示"所有文件夹均不在已折叠集合" → 全部展开（用户反馈 bug）
- 修正后采用 **null 语义**（App.tsx `sidebarCollapsedKeys` 初始 `null`，非 `[]`）：
  - `null` = 从未有过任何折叠记录（全新用户）→ Sidebar 收集当前树全部文件夹 key 加入 `collapsedKeys`，**默认全部折叠**
  - 有记录但与当前树完全不匹配（打开的是新工作区）→ 同样全部折叠；**不写回记录**（折叠全部是幂等操作），用户第一次手动折叠/展开时 `onCollapsedKeysChange` 自然把记录更新为新工作区的键
  - 有匹配记录 → 按记录原样恢复
  - 边界：空数组 `[]` = 用户曾展开全部文件夹，必须保持原样，不能误判为"无匹配"
- `usePersistedSetting` 增加 `value == null` 守卫：null 不写回，避免覆盖其他工作区/其他记录的配置

---

## 三、文件清单

### 新建文件（6 个）

| 文件 | 说明 |
|------|------|
| `src/renderer/src/lib/wiki-resolver.ts` | Wiki 链接目标文件路径解析器 |
| `src/renderer/src/lib/wiki-resolver.test.ts` | 解析器单元测试（9 个用例） |
| `src/renderer/src/lib/frontmatter-parser.ts` | YAML frontmatter 解析/格式化/替换 |
| `src/renderer/src/components/Editor/plugins/wikiLink.ts` | Wiki 链接 ProseMirror 插件集 |
| `src/renderer/src/components/Editor/WikiAutocomplete.tsx` | 自动补全浮层 React 组件 |
| `src/renderer/src/components/Editor/FrontmatterProperties.tsx` | 属性面板 React 组件 |

### 修改文件（5 个）

| 文件 | 修改内容 |
|------|----------|
| `src/renderer/src/components/Editor/index.tsx` | 注册 wiki-link 插件；新增 autocomplete state、wikiLinkFiles/onWikiLinkClick props |
| `src/renderer/src/components/Editor/EditorOverlays.tsx` | 新增 `WikiAcOverlayState` 类型和 `<WikiAutocomplete>` 渲染 |
| `src/renderer/src/components/Sidebar/index.tsx` | 新增受控 collapsedKeys/activeTab props |
| `src/renderer/App.tsx` | 接入 Wiki 链接/属性面板/侧边栏持久化 |
| `src/renderer/src/styles/components/editor.css` | 新增 .wiki-link、.wiki-ac-*、.frontmatter-props、.fm-* 样式 |

---

## 四、核心实现详解

### 4.1 wiki-resolver.ts — 文件路径解析

```typescript
// 解析规则（按优先级）：
// 1. 以 / 开头 → 从工作区根目录拼接（绝对路径）
// 2. 在当前文件目录查找 target.md、target、target/index.md
// 3. 逐级向上查找（子目录可引用父目录文件）
// 4. 全树大小写不敏感文件名匹配

export function resolveWikiTarget(
  target: string,
  workspacePath: string,
  currentFilePath: string | undefined,
  tree: FolderTreeNode[],
): WikiResolveResult
```

**关键函数：**
- `collectMdFiles(tree)` — 展平工作区树为路径列表
- `findFileByName(tree, name)` — 大小写不敏感文件名查找

### 4.2 wikiLink.ts — ProseMirror 插件集

**四个插件 + 一个输入规则：**

```typescript
// 1. wikiLinkSchema — $node('wiki_link', ...)
//    inline atom 节点，attrs: { target, alias }
//    toDOM: <span class="wiki-link" data-target="..." title="...">display</span>

// 2. wikiLinkInputRule — $inputRule
//    正则: /\[\[([^\]|\n]+)(?:\|([^\]\n]+))?\]\]$/
//    匹配即替换为 wiki_link 节点

// 3. wikiTextConvertPlugin — $prose Plugin
//    视图创建后扫描文档文本，正则匹配 [[...]] → 替换为节点
//    递归 setTimeout 避免事务位置漂移

// 4. wikiLinkClickPlugin — $prose Plugin
//    props.handleClick 事件委托，检测 .wiki-link → 调用回调

// 5. wikiAutocompletePlugin — $prose Plugin
//    apply(tr) 检测 [[ → 计算坐标 → 通知 React
```

**React 通信模式：**
```typescript
// 模块级 ref 回调（参照 searchController 模式）
let onWikiLinkClickHandler: ((target: string) => void) | null = null
export function setWikiLinkClickHandler(fn) { onWikiLinkClickHandler = fn }

let onAutocompleteChange: ((state) => void) | null = null
export function setWikiAutocompleteHandler(fn) { onAutocompleteChange = fn }
```

### 4.3 WikiAutocomplete.tsx — 补全浮层

```typescript
// 参照 EditorOverlays 的 codePanel 模式：绝对定位浮层
// 键盘导航：↑↓ 移动、Enter 选择、Escape 关闭
// filterWikiSuggestions(tree, query) — 从工作区树筛选匹配项

export interface WikiSuggestion {
  name: string    // 文件名（不含 .md）
  path: string    // 完整磁盘路径
}
```

### 4.4 frontmatter-parser.ts — YAML 解析

```typescript
// parseFrontmatterYaml(text): 逐行解析 key: value
//   支持：引号包裹值、转义引号、# 注释行
//   不支持：嵌套对象、多行字符串（回退为原始文本）

// formatFrontmatterYaml(props): 序列化为 YAML 行
//   值含特殊字符时自动加双引号 + 转义

// replaceFrontmatter(markdown, props): 替换文档级 frontmatter
//   空 props → 移除 frontmatter 块
//   无 frontmatter → 在文档头添加
```

### 4.5 FrontmatterProperties.tsx — 属性面板

```typescript
// 组件结构：
// <div.frontmatter-props>
//   <button.fm-toggle-btn> 属性 (N) </button>  ← 折叠/展开
//   <div.fm-table>
//     <div.fm-row>  ← 每行：属性名 | 值（可编辑） | 删除按钮
//     <button.fm-add-btn> + 添加属性 </button>
```

**编辑流程：**
1. 点击值 → `startEdit(key)` → 渲染 `<input>` 替代文本
2. Enter/失焦 → `commitEdit(key)` → `onUpdateProperty(key, value)`
3. App.tsx → `replaceFrontmatter(md, newProps)` → `editorRef.replaceContent(newMd)`
4. `onChange` 回调 → App 重新计算 `frontmatterProperties` → 面板重渲染

### 4.6 Sidebar 持久化改造

**控制反转：**

```typescript
// 之前（非受控）：
const [collapsedKeys, setCollapsedKeys] = useState<Set<string>>(new Set())
const [activeTab, setActiveTab] = useState<'files' | 'outline'>('files')

// 之后（受控）：
// Props: initialCollapsedKeys, onCollapsedKeysChange, activeTab, onActiveTabChange
const activeTab = controlledTab ?? localActiveTab
const setActiveTab = onActiveTabChange ?? setLocalActiveTab
const [collapsedKeys, setCollapsedKeys] = useState(() =>
  new Set(initialCollapsedKeys ?? [])
)
```

**App.tsx 持久化：**
```typescript
const [sidebarCollapsedKeys, setSidebarCollapsedKeys] = useState<string[]>([])
usePersistedSetting('sidebarCollapsedKeys', sidebarCollapsedKeys, settingsReady, 500)

const [sidebarActiveTab, setSidebarActiveTab] = useState<'files' | 'outline'>('files')
usePersistedSetting('sidebarActiveTab', sidebarActiveTab, settingsReady)
```

### 4.7 App.tsx 集成

**Wiki 链接点击处理：**
```typescript
const wikiClickOpenRef = useRef(
  (path: string) => { void handleSelectWorkspaceFile(path) }
)
const handleWikiLinkClick = useCallback((target: string) => {
  const result = resolveWikiTarget(target, workspace.path, activeFile?.path, workspace.tree)
  if (result.resolved) {
    wikiClickOpenRef.current(result.path)
  } else {
    setToast(`无法找到链接的目标文件：${target}`)
  }
}, [workspace, activeFile])
```

> 使用 ref 而非直接引用 `handleSelectWorkspaceFile` 避免 useCallback 循环依赖（后者在下方 ~330 行后才定义）。

**自动补全文件列表：**
```typescript
const wikiLinkFileList = useMemo(() => {
  if (!workspace?.tree) return []
  const files = collectMdFiles(workspace.tree)
  return files.map((path) => ({
    name: path.replace(/\\/g, '/').split('/').pop()?.replace(/\.md$/i, '') ?? '',
    path,
  }))
}, [workspace?.tree])
```

**属性面板渲染（内联函数，避免额外 useMemo）：**
```typescript
{activeFile?.path && showFrontmatterProps && activeContent && (
  <FrontmatterProperties
    properties={(() => {
      const extracted = extractFrontmatterRaw(activeContent)
      return extracted ? parseFrontmatterYaml(extracted.text) : null
    })()}
    show={true}
    onToggle={() => setShowFrontmatterProps(v => !v)}
    onUpdateProperty={(key, value) => {
      const props = { ...existing, [key]: value }
      editorRef.current?.replaceContent(replaceFrontmatter(activeContent, props))
    }}
    // ... onDeleteProperty, onAddProperty 类似
  />
)}
```

---

## 五、CSS 样式

```css
/* Wiki 链接 — accent 色背景 + 圆角，hover 下划线 */
.wiki-link {
  color: var(--accent);
  background: var(--accent-bg);
  border-radius: 3px;
  padding: 0 2px;
  cursor: pointer;
}

/* 自动补全浮层 — z-index 50，与 codePanel 同级 */
.wiki-ac-panel {
  position: absolute; z-index: 50;
  min-width: 240px; max-width: 400px;
  background: var(--bg-menu);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-m);
}

/* 属性面板 — 紧凑表格，muted 配色 */
.frontmatter-props { border-bottom: 1px solid var(--border); }
.fm-row { display: flex; gap: 8px; }
.fm-key { min-width: 80px; color: var(--text-3); }
.fm-val-text { cursor: pointer; }         /* 点击编辑 */
.fm-input { border: 1px solid var(--accent); } /* 编辑态高亮 */
.fm-add-btn { border: 1px dashed var(--border); } /* 虚线添加按钮 */
```

---

## 六、数据流图

### Wiki 链接点击

```
用户点击 [[subdir/note]]
  → ProseMirror handleClick → .wiki-link[data-target="subdir/note"]
  → wikiLinkClickPlugin -> onWikiLinkClickHandler("subdir/note")
  → App.handleWikiLinkClick("subdir/note")
  → resolveWikiTarget("subdir/note", workspace, currentPath, tree)
    → 查找 D:/notes/subdir/note.md
  → wikiClickOpenRef.current("D:/notes/subdir/note.md")
  → handleSelectWorkspaceFile("D:/notes/subdir/note.md")
  → IPC 读文件 → 打开标签页 → 切换焦点
```

### 自动补全

```
用户输入 [[
  → wikiAutocompletePlugin.apply() 检测到 [[
  → 提取 query = 光标到 [[ 间的文本
  → coordsAtPos 获取光标坐标
  → onAutocompleteChange({ query, from, to, coords })
  → React setWikiAcState → 派生 wikiAcOverlay
  → <WikiAutocomplete query="xxx" suggestions={filtered} x y />
  → 用户选择 → onSelect(path)
    → ProseMirror 事务替换 [[...text...]] 为 wiki_link 节点
```

### 属性编辑

```
文档加载
  → activeContent 更新
  → extractFrontmatterRaw(activeContent) → { text: "title: Hello\ndate: 2026" }
  → parseFrontmatterYaml(text) → { title: "Hello", date: "2026" }
  → FrontmatterProperties 渲染表格

用户编辑 title 为 "World"
  → onUpdateProperty("title", "World")
  → replaceFrontmatter(activeContent, { title: "World", date: "2026" })
    → "---\ntitle: World\ndate: 2026\n---\n\n# 正文..."
  → editorRef.replaceContent(newMarkdown)
  → Milkdown listenerCtx.markdownUpdated → onChange(newMarkdown)
  → handleEditorChange → setContents → activeContent 更新 → 面板重渲染
```

### 文件树持久化

```
用户折叠 /notes/sub 文件夹
  → toggleCollapse("D:/notes/sub")
  → setCollapsedKeys(prev => prev.add("D:/notes/sub"))
  → onCollapsedKeysChange(Array.from(collapsedKeys))
  → App.setSidebarCollapsedKeys(["D:/notes/sub"])
  → usePersistedSetting 500ms debounce → settings.set('sidebarCollapsedKeys', [...])
  → 主进程 settings-store.ts → 写 settings.json

重启
  → App Promise.all → api.get('sidebarCollapsedKeys') → ["D:/notes/sub"]
  → setSidebarCollapsedKeys(["D:/notes/sub"])
  → SidebarProps.initialCollapsedKeys = ["D:/notes/sub"]
  → new Set(["D:/notes/sub"]) → /notes/sub 显示为折叠
```

---

## 七、关键技术决策

| 决策 | 方案 | 理由 |
|------|------|------|
| Wiki 链接解析方式 | 文档加载后文本扫描转换 | 避免 micromark 扩展的类型复杂度；ProseMirror 事务逐个转换避免位置漂移 |
| 属性编辑写回 | 全文 `replaceContent` | 简单可靠；精准 ProseMirror 节点替换留作优化 |
| 属性面板位置 | 编辑器上方（非侧边栏） | 仿 Obsidian，离编辑内容近；侧边栏已拥挤 |
| 侧边栏状态管理 | 控制反转（受控组件） | 与 `usePersistedSetting` 模式一致；外部可选择不传 props 回退非受控 |
| Wiki 链接 onClick | 模块级 ref + `useEffect` set | 参照 `setLineNumbersEnabled` 模式；避免 Props 穿透多层 |
| YAML 解析 | 自写简单的逐行解析 | 零依赖；覆盖 90% 场景；复杂结构回退为原始文本 |
| 自动补全过滤 | 展平工作区树 + 名称匹配 | 简单高效；无需 IPC，树已在内存中 |

---

## 八、测试

```bash
$ npx vitest run
 Test Files  8 passed (8)
      Tests  31 passed (31)  # 含新增 9 个 wiki-resolver 测试
```

新增测试覆盖：
- `collectMdFiles` — 收集扁平化文件路径
- `findFileByName` — 大小写不敏感查找
- `resolveWikiTarget` — 7 种场景：精确匹配、扩展名、父目录引用、绝对路径、子路径、不存在、空 target

---

## 九、修改文件详情

### `src/renderer/src/components/Editor/index.tsx`

**Import 新增：**
```typescript
import {
  wikiLinkSchema, wikiLinkInputRule, wikiLinkClickPlugin,
  wikiAutocompletePlugin, wikiTextConvertPlugin,
  setWikiLinkClickHandler, setWikiAutocompleteHandler,
  type WikiAutocompleteState,
} from './plugins/wikiLink'
import { filterWikiSuggestions, type WikiSuggestion } from './WikiAutocomplete'
```

**Props 新增：**
```typescript
interface EditorProps {
  // ...原有 props
  wikiLinkFiles?: WikiSuggestion[]
  onWikiLinkClick?: (target: string) => void
}
```

**State 新增：** `wikiAcState` + 派生 `wikiAcOverlay`

**插件注册新增：**
```typescript
.use(wikiLinkSchema)
.use(wikiLinkInputRule)
.use(wikiLinkClickPlugin)
.use(wikiAutocompletePlugin)
.use(wikiTextConvertPlugin)
```

**useEffect 新增：** 2 个（绑定 autocomplete handler + click handler）

### `src/renderer/src/components/Editor/EditorOverlays.tsx`

**新增类型：**
```typescript
export interface WikiAcOverlayState {
  query: string
  suggestions: WikiSuggestion[]
  x: number; y: number
  onSelect: (path: string) => void
  onClose: () => void
}
```

**Props/渲染新增：** `wikiAutocomplete` prop → `<WikiAutocomplete />` 渲染

### `src/renderer/src/components/Sidebar/index.tsx`

**Props 新增：**
```typescript
interface SidebarProps {
  // ...原有 props
  initialCollapsedKeys?: string[]
  onCollapsedKeysChange?: (keys: string[]) => void
  activeTab?: 'files' | 'outline'
  onActiveTabChange?: (tab: 'files' | 'outline') => void
}
```

**State 改造：** `collapsedKeys` 用 `initialCollapsedKeys` 初始化；`activeTab` 支持受控模式

### `src/renderer/App.tsx`

**Import 新增：** `resolveWikiTarget`、`collectMdFiles`、`parseFrontmatterYaml`、`replaceFrontmatter`、`extractFrontmatterRaw`、`FrontmatterProperties`

**State 新增：** `sidebarCollapsedKeys`、`sidebarActiveTab`、`showFrontmatterProps`

**usePersistedSetting 新增：** 3 个（sidebarCollapsedKeys、sidebarActiveTab、showFrontmatterProps）

**启动加载新增：** `api.get('sidebarCollapsedKeys')`、`api.get('sidebarActiveTab')`、`api.get('showFrontmatterProps')`

**回调新增：** `handleWikiLinkClick`、`wikiLinkFileList`（useMemo）

**JSX 新增：** `<FrontmatterProperties>` + Editor 新增 props `wikiLinkFiles`、`onWikiLinkClick`

### `src/renderer/src/styles/components/editor.css`

**新增样式块（~160 行）：**
- `.wiki-link` — 链接按钮样式
- `.wiki-ac-panel` / `.wiki-ac-list` / `.wiki-ac-item` / `.wiki-ac-name` / `.wiki-ac-path` — 自动补全浮层
- `.frontmatter-props` / `.fm-toggle-btn` / `.fm-chevron` / `.fm-count` — 属性面板容器
- `.fm-table` / `.fm-row` / `.fm-key` / `.fm-val` / `.fm-val-text` / `.fm-input` — 属性表格
- `.fm-rm-btn` / `.fm-add-btn` / `.fm-row-new` / `.fm-key-input` — 操作按钮

---

## 十、功能问题修复记录（2026-08-11）

> 用户真实使用后反馈 3 个问题，全部与新增功能相关，逐一根因定位并修复。验证：typecheck 干净、31/31 测试通过、electron-vite build 成功。

### 10.1 Obsidian 写法不兼容（`[[01 事故复盘/_index|01 事故复盘]]`）

**现象**：Obsidian 库里的 wiki 链接（带路径 + `|别名`）在本编辑器不渲染为链接；保存含此类链接的文档会失败。

**根因（两个叠加）**：
1. `wikiTextConvertPlugin` 的 `view()` 只在编辑器创建时跑一次（50ms 后），切换标签页走 `replaceContent` 不会再次触发 → 新文档的 `[[...]]` 始终是纯文本
2. `toMarkdown` runner 用 `state.addNode('wikiLink', ...)` → mdast-util-to-markdown 遇到未知节点类型抛 `Cannot handle unknown node` → 保存即失败

**解决**：
1. 导出 `convertWikiTextInDoc(view)`，`replaceContent` 后手动调用，新文档立即转换
2. 序列化改用 `state.addNode('html', undefined, '[[target|alias]]')` —— 实证测试（node 直跑 mdast-util-to-markdown）确认：html 处理器零转义、原样输出；text 节点会被 `safe()` 转义成 `\[\[...` 不可用

**思路沉淀**：
- "功能能加载 ≠ 功能能保存" —— 检查新语法功能时，序列化（save/getMarkdown）与解析（load）必须成对验证；roundtrip 用真实语料（Obsidian 库文件）而不是理想用例
- Milkdown 序列化器（`SerializerState`）不是 prosemirror-markdown 的 API（没有 `addText`/`escape` 语义），需要直接读 `@milkdown/transformer` 源码 + 对 mdast-util-to-markdown 做黑盒实验，不能凭经验写

### 10.2 多次 Ctrl+Z 内容乱、跨文档串内容

**现象**：连续按 Ctrl+Z 后文档内容混乱；切换文档后再撤销会把上一份文档的内容回退进来。

**根因（两个叠加）**：
1. `replaceAll(markdown)` 默认 `flush=false` → 走 `view.dispatch(tr.replace(0, docSize, slice))`，**全文替换事务进入 undo 历史** → 切换文档后按 Ctrl+Z 撤销的就是这个替换 → 上一份文档内容串进来
2. wiki 批量转换事务（无 meta）也进历史 → 多次 Ctrl+Z 把 wiki 链接还原为文本、历史栈被自动转换污染

**解决**：
1. `replaceAll(markdown, true)`（flush=true）→ 源码确认：`EditorState.create({schema, doc, plugins: prosePluginsCtx})` + `view.updateState(state)` → **所有插件状态重新初始化，undo/redo 历史清空**。切换文档后 Ctrl+Z 不再能跨文档回退
2. 转换事务加 `tr.setMeta('addToHistory', false)`（prosemirror-history 新版不导出 `historyKey`，但字符串 meta 仍被 `filterTransaction` 支持，源码 dist/index.js:274 确认）
3. 验证了 flush 不破坏 listener 插件：其 `state.init` 在重建时重跑（`prevDoc` 重置），`markdownUpdated` 回调（自动保存/脏标记）继续工作；且 `addToHistory: false` 的事务会被 listener 跳过，正好避免转换循环引发无意义的 markdownUpdated 抖动

**思路沉淀**：
- 文档切换类操作必须与用户编辑在 undo 语义上隔离：程序自动执行的全文替换 = 重建状态（flush），自动转换 = 跳过历史（meta）
- 改内部 API 前先读源码确认（`@milkdown/utils/lib/index.js` replaceAll 实现），别凭 API 名猜测语义

### 10.3 新文件夹默认全部展开

**现象**：打开一个新文件夹，所有子文件夹默认展开，与 Obsidian 默认折叠的行为不符。

**根因**：`sidebarCollapsedKeys` 初始 `[]`，Sidebar 的 `collapsedKeys` 空 Set = 全部展开；且 App 无法区分"没有记录"与"全部展开"两种状态。

**解决**（详见 §2.4）：
- App 初始值改为 `null`（无记录）；`usePersistedSetting` 加 null 守卫
- Sidebar 三条规则：无记录 → 全部折叠；记录不匹配当前树（新工作区）→ 全部折叠（不写回，幂等）；记录匹配 → 恢复
- 空数组 `[]` 特殊处理（用户曾展开全部 → 保持原样）

**思路沉淀**：
- 持久化状态必须区分"从未有值"与"有值但为空"——用 `null` 表达三态，`usePersistedSetting` 的 `value == null` 守卫防止 null 覆盖既有配置
- "折叠集合"数据模型下，恢复展开态只能靠"不在集合中"；跨工作区时路径完全不匹配，此时按新工作区处理（全部折叠）才是用户预期的默认值
